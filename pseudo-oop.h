#include "generic.h"

#define enumeration(name) typedef enum name
#define class(name) typedef struct name
#define alias(type,name) typedef type name

typedef gdouble Value;
typedef gboolean Flag;
typedef void None;
